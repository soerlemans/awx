#!/usr/bin/awk -f
BEGIN {
	# file = "/tmp/jfjfafj.txt"
	# getline var < file

	# Parser fails after returning of array subscript
	# Nullptr being returned somehwere?
	# var[name]
	(10 + 10)

	hallo();
	print "Hello" " " "World!"
}
