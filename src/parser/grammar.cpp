#include "grammar.hpp"


// Exported functions:
constexpr auto grammar::is_literal(const TokenType& t_token_type) -> bool
{
  bool result{false};
  return result;
}

constexpr auto grammar::is_lvalue(const TokenType& t_token_type) -> bool
{
  bool result{false};
  return result;
}

constexpr auto grammar::is_rvalue(const TokenType& t_token_type) -> bool
{
  bool result{false};
  return result;
}


